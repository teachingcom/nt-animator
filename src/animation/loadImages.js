
// handles loading an image resource
export default async function loadImages(relativeTo, data) {
	console.log(this)

	// check if the images have been loaded
	const spritesheet = await animator.loadSpritesheet(relativeTo);

	// identify the container
	const isSingleImage = 'image' in data;
	if (isSingleImage) {


	}

	const path = parsePath(resource);

	// for absolute

	// relative
	if (path.isRelative) {

	}

}
